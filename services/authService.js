const { compare } = require("bcryptjs");
const userModel = require("../models/userModel");
const { sign } = require("si");

module.exports = {
  login: async (body) => {
    try {
    } catch (error) {}
  },
};
